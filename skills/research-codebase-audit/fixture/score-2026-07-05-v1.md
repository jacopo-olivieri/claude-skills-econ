# Fixture run scorecard — ~/scratch/rca-run/audit/

Scored 2026-07-05 against `expected_findings.json` (planted fixture), adversarial
matching on mechanism + location, severity gate per the key's `min_severity`.
Registers scored: `claims_register.md`, `code_error_register.md`,
`output_register.md`, `code_review.xlsx` (both content sheets match the
markdown registers; `*_Original` columns correctly excluded).

## Headline

| Metric | Result |
| --- | --- |
| **Recall (must_find)** | **9 / 10** — P-08 mechanism found but scored below `min_severity` |
| **Precision (decoy)** | **PASS** — fig_placebo never flagged in any register or the xlsx |
| **Expected confirmed examples** | **PASS** — both come out clean |

## Per-item results

| Key ID | Mechanism (short) | min_sev | Matching rows | Severity awarded | Verdict |
| --- | --- | --- | --- | --- | --- |
| P-01 | Prose 0.083 vs artifact −0.038 transcription | 3 | C-0001 (abstract), C-0012 (§3); type `transcription`, status `inconsistent` | 4 | **FOUND** |
| P-02 | Village vs household clustering | 3 | E-0005 (`inference_or_se_specification`), C-0011 (`estimation_specification`) | 4 | **FOUND** |
| P-03 | Weighted claim, unweighted code | 3 | E-0006 (`weighting_error`), C-0010 | 3 | **FOUND** |
| P-04 | Bootstrap 200 reps, no `set seed`/stable sort | 2 | E-0008 (`randomness_or_seed_error`) — both the missing seed and the missing sort are named | 3 | **FOUND** |
| P-05 | `keep if waves < 2` inverts the exclusion rule | 4 | E-0002 (`sample_filter_or_flag_error`, sev 4), C-0005 (`sample_count`, sev 3) | 4 | **FOUND** |
| P-06 | make_figures.py reads phantom `panel_v2.csv` | 2 | E-0007 (`stale_or_wrong_path`), C-0015 | 2 | **FOUND** |
| P-07 | `$controls` defined only in a commented line | 3 | E-0004 (`undefined_variable_or_global`), C-0009 — both note the "Household controls: Yes" table line | 3 | **FOUND** |
| P-08 | Income ÷100 vs "thousands" (factor-of-10 unit error) | 3 | E-0001 (`aggregation_or_unit_error`), C-0004 — correct mechanism, location, and factor-of-10 diagnosis | 2 | **MISS (under-severity)** |
| P-09 | README declares absent, unread `rainfall_stations.csv` | 1 | E-0100 (`readme_or_package_mismatch`), C-0007 | 2 | **FOUND** |
| P-10 | `head_name` + GPS coordinates in shipped data | 2 | E-0104 (`pii_or_disclosure_risk`) — also traces PII propagation into `output/panel.*` | 3 | **FOUND** |

**P-08 detail.** The run identified exactly the planted mechanism (comment/paper
say thousands, code divides by 100, Figure 1 axis mislabelled) at exactly the
planted location, in both registers, cross-linked. But it scored severity 2 on
the reasoning that a constant rescaling shifts log income without changing the
slope, so "reproducibility/label defect". The key requires ≥3; the rubric's
severity 3 is "changes a reported number", and every reported income *level*
(and the figure axis) is off by 10×, so the key's floor is defensible. Under the
strict `severity >= min_severity` rule this scores as a miss. It is a severity-
calibration failure, not a detection failure.

## Decoy check (D-01)

`fig_placebo` appears only in three non-register files, each time as an
explicit exclusion, never as a finding:

- `CODEMAP.md` boundary B-0009 — "Commented-out output in paper … None (informational)."
- `plans/claims_review_plan.md` — listed under comment-blanked exclusions, "not claim rows."
- `_work/w01_full_paper.md` — "skip — not PDF-visible."

No claims, code-error, or output row and no xlsx row references the placebo
figure. Comment-blanking worked as designed. **Pass.**

## Expected confirmed examples

1. **N = 4,832** — C-0006 status `confirmed`, no severity, no issue text. Clean.
2. **README per-object script mapping** — no `readme_or_package_mismatch` row
   contests the Table 1 → `analysis.do` or Figure 1 → `make_figures.py` mapping.
   E-0101 cites README's run-order step 3, but its mechanism is the *absent*
   `fig1_income_trends.pdf` artifact, not a wrong mapping — and the key treats
   the P-06 stale-path finding as separate and expected. Clean.

## Register-quality problems the key doesn't capture

1. **Severity calibration on the unit error** (P-08, above) is the run's one
   substantive scoring failure — a rubric-interpretation issue worth a skeleton
   note: "off-by-10 in a reported level is severity 3 even when the headline
   coefficient is unaffected."
2. **Internal contradiction between C-0008 and E-0003.** C-0008 confirms the
   claim that the shock is "the standardised deviation … from the village
   long-run mean," while E-0003 (confirmed, sev 2) says the "long-run mean" is
   actually a two-wave in-sample mean — the same mechanism that C-0008 treats as
   supported. Also E-0003 carries no `Related Claim IDs`; the cross-link stage
   should have paired it with C-0008 (and forced a status re-examination).
3. **Cross-link gap on the bootstrap.** E-0008 (unseeded bootstrap) has no
   `Related Claim IDs`, and the bootstrap claims C-0017 (`confirmed`) and
   C-0018 ("CI excludes zero", `mapped`) carry no `Related Error IDs`. The
   non-reproducibility finding directly undermines C-0018 and should at least
   be linked bidirectionally.
4. **Worker-internal codenames leak into `*_Original` columns.** E-0003's
   `Why It Matters Original` references "CE-02's README lens" and E-0101's
   references "per CE-01's broken input path" — shard-local codenames that
   resolve to nothing in the shipped register. They're excluded from the xlsx
   (author never sees them), but they degrade the audit trail. The author-facing
   rewritten columns themselves read cleanly; no cryptic text reaches the xlsx.
5. **Lint status.** `lint_registers.py --stage b9` **passes**. `--stage b8`
   **fails**: `_staging/claims_register.md` and `_staging/code_error_register.md`
   are missing/empty (the `_staging/` directory exists but is bare). Either the
   staging copies were cleaned up after promotion or the b8 boundary was
   skipped; the final registers are lint-clean, but the run leaves a
   non-replayable b8 boundary behind.
6. **ID gap C-0014** is properly accounted for in the shard footer ("intentionally
   unused — controls folded into C-0009") — noted here only because a scorer
   grepping IDs will hit the gap; no violation.

## Net assessment

Recall 9/10 with the single miss being severity under-scoring on a correctly
diagnosed mechanism; precision and both confirmed-example checks pass; final
registers lint-clean. The two systematic weaknesses to feed back into the
skeletons: severity anchoring for unit/level errors, and cross-link completeness
when a code error contradicts a `confirmed` claim.
